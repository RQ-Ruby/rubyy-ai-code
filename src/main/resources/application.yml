spring:
  application:
    name: rubyy-ai-code
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi????
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.rq.rubyyaicode.controll
